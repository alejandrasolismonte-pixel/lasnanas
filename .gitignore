# Seguridad: los archivos de entorno reales nunca deben entrar al repositorio.
.env
.env.*
!.env.example

# CONFIGURACIÓN GENERADA: contiene únicamente URL y clave publicable, pero no se versiona.
js/supabase-config.js
